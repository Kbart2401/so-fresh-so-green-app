extends layout.pug

block content
  .page-container
    .profile-header
      .profile-icon
      .profile-info
        h2 #{user.name} 🌽
        .email= user.email
        h3: strong Bio
        p #{user.bio}

  